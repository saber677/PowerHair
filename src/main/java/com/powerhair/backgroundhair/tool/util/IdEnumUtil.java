package com.powerhair.backgroundhair.tool.util;

import java.util.Objects;

public class IdEnumUtil {

    public static <T extends Enum> T getByObj(T[] obj) {
        if (!Objects.isNull(obj)) {
            for (T t : obj) {
                if ()
            }
        }
    }

}
}
