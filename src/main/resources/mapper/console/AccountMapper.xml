<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.powerhair.backgroundhair.module.console.mapper.AccountMapper">
    <select id="get" parameterType="java.lang.Long">
        select count(*)
        from account;
    </select>

    <insert id="save" parameterType="com.powerhair.backgroundhair.module.console.domain.Account">
        insert into account

    </insert>
</mapper>